# =============================================================================
# GETTING STARTED
# COPY this file into place, please leave this example as a reference.
# NOTE: Comments in the main config will be removed by the application
#
# cp config/application.yaml.example config/application.yaml
#
# The following parameters are configurable for the install. Commented out keys
# give the default values used. Uncomment the line to manually set it
# =============================================================================

# =============================================================================
# App Base URL
# Specify the base URL to where the application is being hosted. This is used
# by JSON serializers to give the correct URLs to resources
# =============================================================================
app_base_url:


# =============================================================================
# NGINX Server Name
# The name given to nginx as its server name
# =============================================================================
nginx_server_name:

# =============================================================================
# Base Storage Path
# Specify the path on the server where the uploaded files will be stored. Note
# this is different to the system location of some of the file types. The
# system location will be handled via a symlink
# =============================================================================
base_storage_path:

# =============================================================================
# Base Download URL
# This URL should point to a mount that services the storage path of the files
# All files must be downloadable as it is required by the editing feature.
# =============================================================================
base_download_url:

# =============================================================================
# Content Base Path
# Set the locations the internal configuration files will be stored at
# =============================================================================
content_base_path:

# =============================================================================
# UEFI Base System Path
# The base system location to save the uefi files. The filename will
# automatically be prefixed with grub.cfg- followed by the files id
# =============================================================================
uefi_base_system_path:

# =============================================================================
# Pxelinux Base System Path
# The base system location to save the pxelinux config files. The filename is
# the same as the id
# =============================================================================
pxelinux_base_system_path:

# =============================================================================
# Initrd Kernel Base and URL Path
# The base location to store the kernerl and initrd. This should be located
# on some form of file server that can be reached by the base url
# =============================================================================
initrd_kernel_base_path:
initrd_kernel_base_url:

